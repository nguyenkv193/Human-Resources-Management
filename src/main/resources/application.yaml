spring:
  application:
    name: humanresources
  datasource:
    url: jdbc:postgresql://localhost:5436/humanresources_db
    username: postgres
    password: admin
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

